Запишіть SQL-запити для маніпулювання даними з таблиць, що створені у
лабораторній роботі 1. В роботі обов’язково відобразити:
1) тригер на операцію модифікації даних INSERT;
2) тригер на операцію модифікації даних DELETE;
3) тригер на операцію модифікації даних UPDATE;
4) функцію для певного розрахунку (збережену процедуру).
